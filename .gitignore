# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# 3D model assets (large; keep locally, do not commit)
public/**/*.glb
public/**/*.gltf
public/**/*.bin

# Derived from the model files above by `npm run models:scan`, which every npm entry point
# runs first (predev / prebuild / pretype-check). Committing it would only create churn.
src/config/generatedLruModels.ts
